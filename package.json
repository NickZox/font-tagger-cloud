{
  "name": "font-tagger-cloud",
  "version": "1.0.0",
  "description": "Cloud font server for Font Tagger",
  "main": "index.html",
  "scripts": {
    "dev": "vercel dev",
    "deploy": "vercel --prod"
  },
  "engines": {
    "node": ">=18"
  }
}